
// post /company/signup
router.post("/company/signup", companyContoller);

// post /company/signin
router.post("/company/signin", companyContoller);

// post /company/update/:id
router.post("/company/update/:id", isAuthenticated, companyContoller);

// get /company/logout
router.get("/company/logout", isAuthenticated, companyContoller);

// get /branch/all-branchs
router.get("/branch/all-branchs", isAuthenticated,  getAllBranches)

// get /branch/branch/:id
router.get("/branch/branch/:id", isAuthenticated,  getBranche)

// post /branch/create
router.post("/branch/create", isAuthenticated, createBranch)

// post /branch/update/:id
router.post("/branch/update/:id", isAuthenticated, updateBranch)

// post  /branch/delete/:id
router.post("/branch/delete/:id", isAuthenticated, deleteBranch)

// get /employe/all-employees
router.get("/employe/all-employees", isAuthenticated,  getAllEmployees)

// post /employe/create
router.post("/employe/create", isAuthenticated,  createEmployees)

// post  /employe/update/:id
router.post("/employe/update/:id", isAuthenticated, updateEmployees)

// post /employe/delete/:id
router.post("/employe/delete/:id", isAuthenticated, deleteEmployees)

// get /customer/get-all
router.get("/customer/get-all",isAuthenticated, getAllCustomers)

// post /customer/create
router.post("/customer/create",isAuthenticated,  createCustomer)

// post /customer/update/:id
router.post("/customer/update/:id", isAuthenticated, updateCustomers)

// post /customer/delete/:id
router.post("/customer/delete/:id", isAuthenticated, deleteCustomers)

// post /stock/vehicle-sale/create
router.post("/stock/vehicle-sale/create", isAuthenticated, createVehicleSale);

// get /stock/vehicle-sale/all
router.get("/stock/vehicle-sale/all", isAuthenticated, getAllVehicleSales);

// get /stock/purchase/get-all
router.get("/stock/purchase/get-all", isAuthenticated, getAllPurchaseStock);

// post  /stock/purchase/create
router.post("/stock/purchase/create", isAuthenticated, createPurchaseStock);

//post stock/purchase/update/:id
router.post("/stock/purchase/update/:id", isAuthenticated, updatePurchaseStock);

// post stock/purchase/delete/:id
router.post("/stock/purchase/delete/:id", isAuthenticated, deletePurchaseStock);

// get /stock/getAllStock
router.get("/stock/getAllStock", isAuthenticated, getAllStock)

// get /stock/current
router.get("/stock/current", isAuthenticated, currentStock)